# KCD2 depth bridge compatibility entry point

The implementation is shared with other games under
`core/runtime/depth-bridge`. These wrapper scripts keep the historical KCD2
commands working and select the `kcd2` IPC channel automatically.
